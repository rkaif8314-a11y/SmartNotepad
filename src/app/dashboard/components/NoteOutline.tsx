'use client';
import React,{useMemo} from 'react';
import {ListTree} from 'lucide-react';
export default function NoteOutline({content}:{content:string}){const headings=useMemo(()=>{const div=document?.createElement?.('div'); if(!div)return[];div.innerHTML=content;return Array.from(div.querySelectorAll('h1,h2,h3')).map((el,i)=>({id:`outline-${i}`,text:el.textContent?.trim()||'Untitled',level:Number(el.tagName.slice(1))})).filter(x=>x.text);},[content]); return <div className="app-surface p-4"><div className="flex items-center gap-2 mb-3"><ListTree size={15} className="text-primary"/><h3 className="text-xs font-bold">Outline</h3><span className="ml-auto text-[10px] text-muted-foreground">{headings.length}</span></div>{headings.length?<div className="space-y-1">{headings.map(h=><button key={h.id} onClick={()=>document.querySelectorAll('h1,h2,h3')[headings.indexOf(h)]?.scrollIntoView({behavior:'smooth',block:'center'})} className="w-full text-left rounded-lg px-2 py-1.5 text-xs text-muted-foreground hover:bg-muted hover:text-foreground truncate" style={{paddingLeft:`${(h.level-1)*12+8}px`}}>{h.text}</button>)}</div>:<p className="text-[11px] text-muted-foreground">Add headings to build an outline.</p>}</div>;}